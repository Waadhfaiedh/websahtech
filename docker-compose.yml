version: '3.8'

services:
  sahtech-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sahtech-frontend
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    labels:
      - "traefik.enable=false"

networks:
  default:
    name: sahtech-network
